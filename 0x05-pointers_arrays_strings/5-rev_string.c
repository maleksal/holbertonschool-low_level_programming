#include "holberton.h"

/**
  * rev_string - modifies an array
  * @s: arr
  * Return: void
  */

void rev_string(char *s)
{
	int size = 0;
	int index = 0
	int temp;

	while (size != '\0')
	{
		i++;
	}

	for (; i < n / 2; i++)
	{
		temp = s[i];
		s[i] = s[(n - 1) - i];
		s[(n - 1) - i] = temp;
	}
}
