#include "holberton.h"

/**
  *  _abs - computes absolute value of an int
  *  ------------------
  *
  *  @n: takes an int
  *
  *  Return: return absolute value
  *
 **/

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	} return (n *= -1);
}
